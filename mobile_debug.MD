Вот краткая и готовая инструкция для отладки сайта на телефоне с включенным VPN:

1. **Первое окно терминала (Django):**
Запустите сервер, явно указав адрес и порт:
```bash
python manage.py runserver 0.0.0.0:8000

```


*(Убедитесь, что в `settings.py` прописано `ALLOWED_HOSTS = ['*']`).*
2. **Второе окно терминала (Туннель Pinggy):**
Запустите туннель с привязкой к IPv4:
```bash
ssh -p 80 -R 0:127.0.0.1:8000 free@a.pinggy.io

```


*(Приглашение на ввод пароля пропустите, нажав Enter).*
3. **Телефон:**
Скопируйте появившуюся ссылку вида `[https://xxxx.free.pinggy.net](https://xxxx.free.pinggy.net)` (или `*.run.pinggy-free.link`), откройте её на айфоне — сайт заработает.