[Unit]
Description=gunicorn daemon for Django project
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/var/www/safeisol
ExecStart=/var/www/safeisol/venv/bin/gunicorn --workers 3 --bind 127.0.0.1:8000 safeisol.wsgi:application

[Install]
WantedBy=multi-user.target
